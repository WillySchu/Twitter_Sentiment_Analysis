extends ../layout

block content
  .buffer-div
  form(action='results/#{id}' method='post')
    input(type='submit' value='Add this search to ')
  ul
    if results
      each result in results
        li= result.tweet.user.screen_name
          ul
            li= result.tweet.text
            li= result.score
